using System;
using System.Collections;
using System.Collections.Generic;
using System.Security.Cryptography.X509Certificates;

namespace Markdown
{
    public class Md
    {
        public string Render(string text)
        {
            throw new NotImplementedException();
        }

        private IEnumerable<TagToken> ParseTokens(IEnumerable<string> paragraphs)
        {
            throw new NotImplementedException();
        }

        private string ReplaceMarkDownOnTags(string text, IEnumerable<TagToken> tagTokens)
        {
            throw new NotImplementedException();
        }
    }
}