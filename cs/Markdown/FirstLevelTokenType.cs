namespace Markdown
{
    public enum FirstLevelTokenType
    {
        String,
        StringWithNumbers,
        Space,
        Underscore, //нижнее подчёркивание
        Lattice, //решётка
        Backslash, //обратный слэш
    }
}