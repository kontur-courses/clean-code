namespace Markdown
{
    public enum FirstTokenType
    {
        String,
        Space,
        Underscore, //нижнее подчёркивание
        Lattice, //решётка
        Backslash, //обратный слэш
        NewLine
    }
}