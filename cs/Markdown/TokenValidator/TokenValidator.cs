using Markdown.Tokens;

namespace Markdown.TokenValidator;

public class TokenValidator
{
    public IEnumerable<Token> Validate(IEnumerable<Token> tokens)
    {
        return new List<Token>();
    }
    
    
}