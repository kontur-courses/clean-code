namespace Markdown
{
    public enum TokenType
    {
        Text,
        Italics,
        Strong,
        Escape,
        NewLine,
        Header1,
        SquareBracketOpen,
        SquareBracketClose,
        BracketClose,
        BracketOpen,
        Link,
        WhiteSpace
    }
}